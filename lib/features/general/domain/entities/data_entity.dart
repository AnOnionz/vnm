class BaseEntity {
  // @Index(unique: true, replace: true)
  // int? attendanceId;
  // @Index(unique: true, replace: true)
  // int? featureId;

  // DataEntity({this.attendanceId, this.featureId});
}
